// Linear Search algorithm by user input

#include <stdio.h>

int main()
{
    int a[5], i, found = 0, x; 
    printf("Enter the elements in Array : ");
    
    for(i = 0; i < 5; i++)
    {
        scanf("%d", a[i] );
    }
}